import React from 'react';
import { LoadingStyle, LoadingAnimation } from './loading.styles';

const loading = () => {
  return (
    <LoadingStyle>
      <LoadingAnimation></LoadingAnimation>
    </LoadingStyle>
  );
};

export default loading;
